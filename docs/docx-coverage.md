# DOCX coverage snapshot

Generated by `cargo test --package s1engine --test docx_coverage`. The
test reads every fixture in `testdocs/docx/eigenpal/` (mirrored from
`docx-editor/e2e/fixtures/`), round-trips it through Casual Core, and
classifies each XML tag into one of three buckets.

**Methodology** matches the consumer's
[`roundtrip-audit-report.md`](../testdocs/docx/eigenpal/roundtrip-audit-report.md):
a tag is "dropped" only when the output count is exactly 0. Reduced counts
caused by writer consolidation (run merging, etc.) are not flagged.

## Snapshot — initial baseline

```
Fixtures total : 39
  parsed       : 39  (100 %)
  re-written   : 39  (100 %)
  zero-drop    : 10 / 39  (26 %)
```

| Bucket | Tags | Total drops | Meaning |
| --- | ---: | ---: | --- |
| A — consumer supports, we drop | **140** | several thousand | The gap. Closing this enables integration. |
| B — we support, consumer drops | 0 | 0 | We don't lead anywhere yet. |
| C — neither supports | 22 | ~130 | Joint backlog. Fields, math, advanced numbering. |

## What this means

The numbers settle the architecture question:

- **140 tags is too many to hand-code.** At one tag per day, that's six
  months of work, and most of those tags are deep DrawingML / VML / SDT
  structures that nobody will ever map by hand into `s1-model`.
- **The dominant tag families are precisely the kind that benefit from
  pass-through preservation:** DrawingML (`a:*`), VML (`v:*`), DrawingML
  positioning wrappers (`wp:*`), Word Processing Shape (`wps:*`),
  structured-document tags (`w:sdt*`), and complex-script formatting
  properties (`w:bCs`, `w:szCs`, …).
- The consumer's audit lists 22 truly-dropped tags. We share **all 22**
  (Bucket C). The other 140 are tags the consumer's `DocxPackage` AST
  preserves opaquely without their ProseMirror schema needing to
  understand them.

Conclusion: a `s1-ooxml::Package` preservation layer (see
[`integration-plan.md`](integration-plan.md) Option B) is the right
architecture. Hand-mapping every Bucket A tag isn't.

## Bucket A — the gap (top 20 by drop count)

The largest classes of tag we drop today. Pass-through preservation will
close most of these without requiring per-tag mapping.

| Tag | Total drops | Class |
| --- | ---: | --- |
| `w:w` | 3672 | Table cell width |
| `w:szCs` | 1190 | Complex-script font size |
| `w:bCs` | 549 | Complex-script bold |
| `w:proofErr` | 544 | Proofing error marker |
| `w:tcMar` | 333 | Table cell margins |
| `w:position` | 289 | Vertical text position |
| `w:cnfStyle` | 205 | Conditional formatting style ref |
| `w:noProof` | 175 | Suppress proofing |
| `w:trHeight` | 161 | Table row height |
| `w:webHidden` | 119 | Web-hidden marker |
| `w:iCs` | 116 | Complex-script italic |
| `w:trPr` | 113 | Table row properties wrapper |
| `w:kern` | 96 | Character kerning |
| `w:adjustRightInd` | 84 | Auto-adjust right indent |
| `w:autoSpaceDE` | 84 | Auto-space East Asian |
| `w:autoSpaceDN` | 84 | Auto-space ditto |
| `w:tblLook` | 73 | Table conditional formatting |
| `w:cantSplit` | 68 | Don't split row across pages |
| `w:hideMark` | 67 | Hide end-of-cell mark |
| `w:widowControl` | 59 | Widow/orphan control |

Full report at `target/docx-coverage.json` after every test run.

## Bucket C — joint backlog (matches consumer's known gaps)

| Tag | Drops | What it is |
| --- | ---: | --- |
| `w:fldChar` | 60 | Field code start/end marker — TOCs, page numbers |
| `w:instrText` | 19 | Field code instruction text |
| `w:delInstrText` | 1 | Deleted instruction (track changes + fields) |
| `w:ind` | 10 | Paragraph indentation |
| `w:sdt`* | 8 each | Content controls (structured-document tags) |
| `w:highlight` | 2 | Run highlight color |
| `w:rPr` | 6 | Run properties container |
| `w:rStyle` | 4 | Run style reference |
| `w:spacing` | 5 | Run spacing |
| `w:start` / `w:end` | 4 each | Paragraph border start/end |
| `w:pBdr` | 5 | Paragraph border |
| `w:pgNumType` | 4 | Page number type |
| `w:formProt` | 2 | Form protection |
| `w:textDirection` | 2 | Text direction |
| `w:bdr` | 1 | Border value |
| `w:bookmarkEnd` | 1 | Bookmark end marker |
| `w:footnotePr` / `w:endnotePr` | 1 each | Footnote/endnote properties |
| `wp14:pctHeight`/`Width` | 2 each | Relative sizing |
| `wp14:sizeRelH`/`V` | 2 each | Size-relative-to anchor |

## Where this number needs to go

Per the [integration plan](integration-plan.md) phase gates:

| Phase | Bucket A target |
| --- | --- |
| Phase 1 (today) | 140 (baseline) |
| Phase 2 (Casual Core ready) | 0 |
| Phase 3 (shadow parsing in editor) | 0 |
| Phase 4 (element-level migration) | 0 |

The path from 140 → 0 is the `s1-ooxml::Package` preservation layer, not
140 individual element implementations.
